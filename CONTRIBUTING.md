# Contributing to p98lib

p98lib は「公開資料の実測+自前の実測結果」だけをもとに作られています。この
独立性を保つことを最優先にしているため、以下のものは **受け取りません**。

## 受け取らないもの

### 1. 他人の実装の逆アセンブル結果・移植・そこから写したコード

`master.lib` などの既存 PC-98 ライブラリの実装(逆アセンブル結果、ソース、
そこから写した/参考にしたコードやコメント)は受け取りません。仕様の裏付けとして
それらを参照した旨のコメントが含まれる PR も同様です。

理由: 本ライブラリはハードウェア仕様を third-party ライブラリの実装からではなく、
公開資料と自前の実測から独立に導出する方針で作られています。他人の実装を一部でも
取り込むと、この独立性(および由来の明確さ)が失われます。

### 2. 出典不明の仕様記述

「〜という仕様のはず」「一般的にこうなっている」といった、出典を示せない仕様の
記述は受け取りません。仕様の根拠として認めるのは次のいずれかです:

- **公開資料**(WebNP2-wiki、公式マニュアル等)。参照した資料名と該当箇所を明記してください。
- **自分で測った結果**。この場合は**測り方も一緒に書いてください**
  (何を・どういう条件で・どうやって測ったか。`docs/design.md` の実測の書き方を
  参考にしてください)。

### 3. 権利の確認できない画像・音楽等の素材

出所や権利関係を確認できない画像・音楽等の素材(変換前のファイル・変換済みの
データのどちらも)は受け取りません。素材を追加する PR では、その素材が誰の
著作物で、どういう経緯で使ってよいと判断したのかを `docs/assets.md` の書式に
合わせて明記してください。判断に迷う場合は取り込まず、Issue 等で報告してください。

このリポジトリには pre-commit フックによる機械的な混入検査もあります
(`tools/check_assets.mjs`)。`docs/assets.md` の「使ってよい」一覧に無い素材由来の
データはコミット時に自動的に弾かれます。有効化方法は `README.md` を参照してください。

## PR を送る前に

- `node tools/verify.mjs` を通してください。
- 実測に基づく主張には、測定条件(何を測ったか・どの環境で・何回)を明記してください。
- 「未確認」「実機未検証」であることを隠さず、正直に書いてください。SKIP や
  未実施の検証を合格扱いにしないでください。
